namespace Examples.Models
{
    public enum ParticipantActive
    {
        Inactive = 0,
        Active = 1
    }
    public enum ParticipantPhase
    {
        Initialization = 0,
        Processing = 1,
        Review = 2,
        Approval = 3,
        Rejection = 4
    }
}
